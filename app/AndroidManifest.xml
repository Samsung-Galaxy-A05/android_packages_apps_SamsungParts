<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.samsungparts"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SamsungParts">
        <activity
            android:name=".BatterySettings"
            android:exported="true"
            android:noHistory="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SamsungParts">
        </activity>

        <activity-alias
            android:name=".BatterySettings"
            android:targetActivity=".BatterySettings"
            android:exported="true"
            android:label="@string/protect_battery_title">

            <intent-filter>
                <action android:name="com.android.settings.action.IA_SETTINGS"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>

            <meta-data
                android:name="com.android.settings.category"
                android:value="com.android.settings.category.ia.battery"/>

            <meta-data
                android:name="com.android.settings.order"
                android:value="999"/>

            <meta-data
                android:name="com.android.settings.summary"
                android:resource="@string/protect_battery_summary"/>

            <meta-data
                android:name="com.android.settings.profile"
                android:value="primary_profile_only"/>
        </activity-alias>

    </application>

</manifest>
